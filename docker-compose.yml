services:
  fradel-lodge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fradel-lodge-salesite
    restart: unless-stopped
    ports:
      - "9027:80"
    networks:
      - salesite

networks:
  salesite:
    external: true