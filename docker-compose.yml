services:
  walfinch-ealing-uxbridge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: walfinch-ealing-uxbridge-salesite
    restart: unless-stopped
    ports:
      - "9063:80"
    networks:
      - salesite

networks:
  salesite:
    external: true
