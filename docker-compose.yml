services:
  hcf-the-springs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hcf-the-springs-salesite
    restart: unless-stopped
    ports:
      - "9024:80"
    networks:
      - salesite

networks:
  salesite:
    external: true
