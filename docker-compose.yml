services:
  thurrock:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: thurrock-salesite
    restart: unless-stopped
    ports:
      - "9068:80"
    networks:
      - salesite

networks:
  salesite:
    external: true
