services:
  wickham-hall:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wickham-hall-salesite
    restart: unless-stopped
    ports:
      - "9003:80"
    networks:
      - salesite

networks:
  salesite:
    external: true
